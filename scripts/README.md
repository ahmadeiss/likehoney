# Scripts

Repository-local helpers. Phase 0 keeps this intentionally light — all common
operations are plain pnpm scripts at the workspace root (see the root
`package.json` and `README.md`).

Add scripts here only when a task is genuinely shared and would otherwise be
copy-pasted between developers or CI.
